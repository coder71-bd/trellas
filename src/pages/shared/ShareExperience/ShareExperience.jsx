import React from 'react';

const ShareExperience = () => {
  return (
    <section style={{ minHeight: 'calc(100vh - 100px)' }}>
      In this page the user and admin will share experience. but if the user is
      not admin then he have to wait for the admin approval
    </section>
  );
};

export default ShareExperience;
