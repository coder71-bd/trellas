import React from 'react';

const BlogCard = () => {
  return (
    <div>
      this is the blog card component. If the user is admin then he will see the
      update and delete options
    </div>
  );
};

export default BlogCard;
